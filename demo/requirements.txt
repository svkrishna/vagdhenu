# Hugging Face ZeroGPU Space deps. gradio is provided by the Space SDK (see README.md header).
# torch pinned to 2.8.0 because ZeroGPU only supports torch >= 2.8 (2.8.0/2.9.1/2.10.0/2.11.0);
# the local-install env (repo-root requirements.txt) stays on the validated torch 2.4.1.
spaces
torch==2.8.0
torchaudio==2.8.0
f5-tts
transformers==4.46.3
accelerate==0.34.2
librosa==0.11.0
soundfile
numpy
scipy
huggingface_hub
indic-transliteration
# NVIDIA BigVGAN is NOT a pip package (no setup.py) — app.py clones it at startup and adds it to
# sys.path; we use the torch (non-CUDA-kernel) path so nothing needs compiling.
